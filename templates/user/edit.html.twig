
{% extends 'base.html.twig' %}

{% block title %}Modifier Profil{% endblock %}

{% block body %}
    <h1>Modifier les informations de l'utilisateur</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Mettre à jour</button>
    {{ form_end(form) }}
    <img src="{{ vich_uploader_asset(app.user.userProfile) }}" alt="">

{% endblock %}
