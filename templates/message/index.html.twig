{% extends 'base.html.twig' %}

{% block title %}Messagerie{% endblock %}

{% block body %}
    <main>
        <section class="message-section">
            <h2>Mes messages</h2>
            <p>Vous avez {{ messages.getTotalItemCount }} messages</p>

            <table class="message-table">
                <thead>
                <tr>
                    <th>De</th>
                    <th>Sujet</th>
                    <th>Date</th>
                    <th>Actions</th>
                </tr>
                </thead>
                <tbody>
                {% for message in messages %}
                    <tr class="{% if not message.isRead %}unread{% else %}read{% endif %}">
                        <td>
                            {{ message.sender.userProfile.userName ?: message.sender.userProfile.firstName ?: message.sender.email }}
                        </td>
                        <td>
                            <a href="{{ path('message_read', {id: message.id}) }}">{{ message.title }}</a>
                        </td>
                        <td>
                            {{ message.createdAt|date('d/m/Y H:i') }}
                        </td>
                        <td>
                            <form action="{{ path('message_read', {id: message.id}) }}" method="get" class="view-form">
                                <button type="submit" class="view-btn">Voir</button>
                            </form>

                            <form action="{{ path('message_delete', {id: message.id}) }}" method="post" class="delete-form">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message.id) }}">
                                <button type="submit" class="delete-btn">Supprimer</button>
                            </form>

                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="4">Aucun message reçu.</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
            {% include '_partials/_pagination.html.twig' with {'pagination': messages} %}
        </section>
    </main>
{% endblock %}
